# Scrapers por fonte

Cada fonte nova (site, banco de provas, PDF) ganha um módulo aqui, e todos
convergem no mesmo formato de saída: um CSV no shape documentado no README
do projeto (mesmo lido por `../import_questions.py`).

Nenhum scraper implementado ainda.
